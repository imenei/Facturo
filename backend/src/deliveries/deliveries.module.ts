import { Module } from '@nestjs/common';

// Deliveries are tracked via Invoice deliveryStatus field
// This module is a placeholder for future delivery-specific features
@Module({})
export class DeliveriesModule {}
